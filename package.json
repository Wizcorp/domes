{
  "name": "domes",
  "version": "0.1.2",
  "description": "Evented data manager",
  "repository": "Wizcorp/domes",
  "main": "index.js",
  "scripts": {
    "test": "eslint . && tape test/*.js | tap-spec",
    "cover": "rm -rf .nyc_output && nyc --silent --all tape 'test/*.js' > /dev/null 2>&1 && nyc report"
  },
  "keywords": [
    "evented",
    "data",
    "domes",
    "tomes",
    "dome",
    "tome",
    "diff"
  ],
  "author": "Ron Korving <rkorving@wizcorp.jp>",
  "license": "MIT",
  "dependencies": {
    "clone": "^1.0.2"
  },
  "devDependencies": {
    "eslint": "^2.5.3",
    "nyc": "^6.1.1",
    "tap-spec": "^4.1.1",
    "tape": "^4.5.1"
  }
}
